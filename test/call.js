'use strict';

var should = require('should'),
    call = require('../reflekt').call;

describe('call', function() {
    it('should call the result of calling decorate with the given arguments', function() {
    });
});
