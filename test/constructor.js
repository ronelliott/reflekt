'use strict';

var should = require('should'),
    reflekt = require('../reflekt');

describe('constructor', function() {
    it('should return a function that calls construct with the given args', function() {
    });
});
